generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Synset {
  id         Int     @id @default(autoincrement())
  path       String  @unique
  size       Int
  depth      Int
  parentPath String? @map("parent_path")
  name       String

  @@index([parentPath], name: "idx_parent_path")
  @@index([name], name: "idx_name_trgm", type: Gin)
  @@map("synsets")
}
